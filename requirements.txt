# Torrent Streaming Player — Dependencies
# Install with:  pip install -r requirements.txt

PySide6-Essentials>=6.6.0

# Torrent engine — use 'libtorrent' on most platforms.
# If it fails, try: pip install python-libtorrent
libtorrent>=2.0.0

qrcode>=7.4.2
Pillow>=10.0.0
pychromecast>=13.0.0
